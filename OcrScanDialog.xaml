<Window x:Class="SOLUM_UI.OcrScanDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="520" Height="340"
        ShowInTaskbar="False">

    <Border Background="#44000000">
        <Border Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="32" ShadowDepth="0" Opacity="0.25"/>
            </Border.Effect>
            <Border Background="White" CornerRadius="16" Padding="40,36" Width="480">
                <StackPanel HorizontalAlignment="Center">

                    <Viewbox Width="56" Height="56" Margin="0,0,0,20">
                        <Canvas Width="24" Height="24">
                            <Path Fill="#D0B0BA"
                                  Data="M4,4H7L9,2H15L17,4H20A2,2 0 0,1 22,6V18A2,2 0 0,1 20,20H4A2,2 0 0,1 2,18V6A2,2 0 0,1 4,4M12,7A5,5 0 0,0 7,12A5,5 0 0,0 12,17A5,5 0 0,0 17,12A5,5 0 0,0 12,7M12,9A3,3 0 0,1 15,12A3,3 0 0,1 12,15A3,3 0 0,1 9,12A3,3 0 0,1 12,9Z"/>
                        </Canvas>
                    </Viewbox>

                    <TextBlock Text="OCR / Document Scan" FontSize="18" FontWeight="Bold"
                               Foreground="#702943" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <Border Background="#F9F2F4" CornerRadius="10" Padding="24,18" Margin="0,0,0,24">
                        <TextBlock Text="This feature is currently under development." FontSize="14"
                                   Foreground="#666666" HorizontalAlignment="Center"
                                   TextWrapping="Wrap" TextAlignment="Center"/>
                    </Border>

                    <Button Content="Close" Click="Close_Click"
                            Height="38" Width="140"
                            Background="#702943" Foreground="White"
                            FontSize="13" FontWeight="SemiBold"
                            BorderThickness="0" Cursor="Hand"
                            HorizontalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bg" Property="Opacity" Value="0.88"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </Border>
        </Border>
    </Border>
</Window>
