<Window x:Class="SOLUM_UI.ToastNotification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="320"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Border x:Name="RootBorder"
            Background="White"
            CornerRadius="12"
            Margin="6,4,6,4">
        <Border.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" X="340" Y="0"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="3" Opacity="0.14"/>
        </Border.Effect>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetName="SlideTransform"
                            Storyboard.TargetProperty="X"
                            From="340" To="0"
                            Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="28"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="AccentBar"
                    Grid.Column="0"
                    CornerRadius="12,0,0,12"
                    Background="#702943"/>
            <Border Grid.Column="1"
                    Width="28" Height="28"
                    CornerRadius="14"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    x:Name="IconCircle"
                    Background="#702943">
                <TextBlock x:Name="TxtIcon"
                           Text="✓"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="2" Margin="10,12,4,12" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTitle"
                           FontFamily="Segoe UI"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#1A1A1A"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtMessage"
                           FontFamily="Segoe UI"
                           FontSize="11"
                           Foreground="#555555"
                           TextWrapping="Wrap"
                           Margin="0,3,0,0"/>
            </StackPanel>
            <Button Grid.Column="3"
                    Width="24" Height="24"
                    VerticalAlignment="Top"
                    Margin="0,6,4,0"
                    Cursor="Hand"
                    Click="CloseBtn_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <TextBlock Text="✕" FontSize="10" Foreground="#888888"/>
            </Button>
        </Grid>
    </Border>
</Window>
